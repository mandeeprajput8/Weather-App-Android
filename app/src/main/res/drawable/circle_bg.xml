<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Transparent fill -->
    <solid android:color="#22FFFFFF"/>

    <!-- Outer glow (soft white border) -->
    <stroke
        android:width="3dp"
        android:color="#AAFFFFFF"/>

    <!-- Rounded circle -->
    <corners android:radius="200dp"/>

</shape>